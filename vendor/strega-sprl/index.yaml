# This example contains just one end device: smartvalve. It is referenced here in the index.

endDevices:
  # Unique identifier of the end device (lowercase, alphanumeric with dashes, max 36 characters)
  - smart-valve # look in smart-valve.yaml for the end device definition
