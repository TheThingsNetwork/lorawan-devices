name: Sound Level Sensor
description: The Browan Sound Level Sensor integrates a MEMS microphone for measuring sound levels in decibels (dBA). It utilizes LoRaWAN® connectivity to transmit data and is suitable for use in a variety of building environments.

hardwareVersions:
  - version: '1.0'
    numeric: 1

firmwareVersions:
  - # Firmware version
    version: '1.0.1' # unknown
    numeric: 1
    hardwareVersions:
      - '1.0'

    profiles:
      EU863-870:
        id: tabs-profile
        lorawanCertified: true
        codec: tbsl100-codec
      US902-928:
        id: tabs-profile
        lorawanCertified: true
        codec: tbsl100-codec

sensors:
  - sound

dimensions:
  width: 20
  length: 50
  height: 50

weight: 40

battery:
  replaceable: true
  type: 3.6V 1⁄2 AA Li-SOCl2 1200mAh

operatingConditions:
  temperature:
    min: 0
    max: 50

ipCode: IP40

productURL: http://www.browan.com/product/sound-level-sensor/detail

photos:
  main: tbsl100.png

keySecurity: none

keyProvisioning:
  - manifest
