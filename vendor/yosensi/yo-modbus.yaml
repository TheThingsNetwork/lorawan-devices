name: YO Modbus - the bridge between the Modbus network and LoRaWAN | LoRa EU868, US915, AU915, AND AS923
description: YO Modbus is the bridge between the Modbus network and LoRaWAN. It allows to read data via Modbus RTU from Slave devices and send it via LoRaWAN.
With one YO Modbus device, it is possible to create up to 150 Modbus queries and send via LoRaWAN collected in up to 30 different LoRa frames. There is no limit to the number of devices to which your Modbus can send queries. The limit is only based on the number of queries.
The device is designed for mounting on standard 35 mm DIN rails in electrical switchboards. It occupies only one pole width like a standard single-phase overcurrent circuit breaker.
The RS485 input on which the Modbus RTU protocol works is galvanically isolated from the rest of the device modules for maximum protection.
Additionally, the device can be connected to protective earth (PE) cable so that the charges coming from the RS485 cable shielding will be slowly dissipated to the earth cable. It will lead to equipotential bonding in the Modbus network on the device side.
It can be powered by direct or alternating current over a wide voltage range of 6-30 VDC or 5-21 VAC respectively.
The device communicates wirelessly over LoRaWAN. It is high-quality product made in EU.

# Hardware versions (optional, use when you have revisions)
hardwareVersions:
  - version: '1.0'
    numeric: 1

# Firmware versions (at least one is mandatory)
firmwareVersions:
  - # Firmware version
    version: '3.1.0'
    numeric: 1
    hardwareVersions:
      - '1.0'

    # LoRaWAN Device Profiles per region
    # Supported regions are EU863-870, US902-928, AU915-928, AS923, CN779-787, EU433, CN470-510, KR920-923, IN865-867,
    # RU864-870
    profiles:
      EU863-870:
        # Optional identifier of the vendor of the profile. When you specify the vendorID, the profile is loaded from
        # the vendorID's folder. This allows you to reuse profiles from module or LoRaWAN end device stack vendors.
        # If vendorID is empty, the current vendor ID is used. In this example, the vendorID is the current vendor ID,
        # which is verbose.
        #vendorID: 659
        # Identifier of the profile (lowercase, alphanumeric with dashes, max 36 characters)
        id: yo-modbus-profile
        lorawanCertified: true
      US902-928: #dodać profil na US
        id: yo-modbus-profile-us
        lorawanCertified: true
      AU915-928:
        id: yo-modbus-profile-au
        lorawanCertified: true

# Sensors that this device features (optional)
# Valid values are:
# 4-20 ma, accelerometer, altitude, analog input, auxiliary, barometer, battery, button, bvoc, co, co2, conductivity,
# current, digital input, dissolved oxygen, distance, dust, energy, gps, gyroscope, h2s, humidity, iaq, level, light,
# lightning, link, magnetometer, moisture, motion, no, no2, o3, particulate matter, ph, pir, pm2.5, pm10, potentiometer,
# power, precipitation, pressure, proximity, pulse count, pulse frequency, radar, rainfall, rssi, smart valve, snr, so2,
# solar radiation, sound, strain, surface temperature, temperature, tilt, time, tvoc, uv, vapor pressure, velocity,
# vibration, voltage, water potential, water, weight, wifi ssid, wind direction, wind speed.
<<<<<<< HEAD
sensors:
  - current
  - voltage
  - pulse frequency
  - digital input
  - energy
  - power
  

=======
#sensors:
# - modbus
>>>>>>> 512de36373499236772b22d305386ef8481ca23b

# Additional radios that this device has (optional)
# Valid values are: ble, nfc, wifi, cellular.
additionalRadios:
  - ble

# Dimensions in mm (optional)
# Use width, height, length and/or diameter
dimensions:
  width: 17.5
  length: 58
  height: 90

# Weight in grams (optional)
weight: 90

# IP rating (optional)
ipCode: IP20

# Product and data sheet URLs (optional)
productURL: https://yosensi.io/products/yo_modbus/
dataSheetURL: https://yosensi.io/support/datasheets/YO_Modbus_datasheet.pdf

# Photos
photos:
  main: yo-modbus.png
