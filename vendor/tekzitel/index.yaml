# This contains just one end device: tekzipark. It is referenced here in the index.

endDevices:
  # Unique identifier of the end device (lowercase, alphanumeric with dashes, max 36 characters)
  - tekzipark # look in tekzipark.yaml for the end device definition
