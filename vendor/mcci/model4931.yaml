name: Model 4931 Outdoor Sensor
description: Temperature, humidity, pressure, rainfall, soil temperature and moisture, tree temperature and pressure, sap flow

# Hardware versions (optional, used for revisions)
# hardwareVersions:
#  - version: 'A'
#    numeric: 1
#  - version: 'A-1'
#    numeric: 2

# Firmware versions (at least one mandatory)
firmwareVersions:
  - # Firmware version
    version: '1.3.1'
    numeric: 1
    #   corresponding hardware versions (optional)
    #   hardwareVersions:
    #     - '1.0'

    # LoRaWAN device profiles per region
    # defined regions are EU863-870, US902-928, AU915-928, AS923, CN779-787, EU433, CN470-510, KR920-923, IN865-867, RU864-870
    profiles:
      EU863-870:
        # unique ID of the profile (lowercase, alpha with dashes, max 36)
        id: model4931-profile
        lorawanCertified: false
        codec: codec-model4931
      US902-928:
        id: model4931-profile
        lorawanCertified: false
        codec: codec-model4931
      AU915-928:
        id: model4931-profile
        lorawanCertified: false
        codec: codec-model4931
      AS923:
        id: model4931-profile
        lorawanCertified: false
        codec: codec-model4931
      KR920-923:
        id: model4931-profile
        lorawanCertified: false
        codec: codec-model4931
      IN865-867:
        id: model4931-profile
        lorawanCertified: false
        codec: codec-model4931

# Sensors that this device features (optional)
# Valid values are:
# accelerometer, altitude, analog input, auxiliary, barometer, battery, button, bvoc, co2, current, digital input,
# distance, dust, energy, gps, gyroscope, humidity, light, link, magnetometer, moisture, ph, pir, power, pressure,
# proximity, pulse count, pulse frequency, rainfall, rssi, snr, solar radiation, sound, temperature, time, tvoc, uv,
# velocity, vibration, voltage, water, wifi ssid, wind direction, wind speed.
sensors:
  - temperature
  - humidity
  - pressure
  - rainfall
  - pulse count
  - battery

# Dimensions in mm (optional)
# Use width, height, length and/or diameter
dimensions:
  width: 250
  length: 170
  height: 100

##  # Weight in grams (optional)
##  weight: 350

# Battery information (optional)
battery:
  replaceable: true
  type: Li-ion cylindrical (ICR18650)

# Operating conditions (optional)
operatingConditions:
  # Temperature (Celcius)
  temperature:
    min: -20
    max: 70
  # Relative humidity (fraction of 1)
  relativeHumidity:
    min: 0
    max: 0.95

## # IP rating (optional)
## ipCode: IP64

# Key provisioning (optional)
# Valid values are: custom (user can configure keys), join server and manifest.
keyProvisioning:
  - custom
##- join server

# Key security (optional)
# Valid values are: none, read protected and secure element.
keySecurity: secure element

# Product and data sheet URLs (optional)
productURL: https://mcci.io/m4931
dataSheetURL: https://cdn.shopify.com/s/files/1/0873/5104/files/model483x-datasheet.pdf?v=1610937656
## resellerURLs:
##   - name: 'Reseller 1'
##     region:
##       - European Union
##     url: https://example.org/reseller1
##   - name: 'Reseller 2'
##     region:
##       - United States
##       - Canada
##     url: https://example.org/reseller2

# Photos
photos:
  main: model4931.png
