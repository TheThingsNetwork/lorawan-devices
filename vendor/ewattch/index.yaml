# This example contains just one end device: windsensor. It is referenced here in the index.

endDevices:
  # Unique identifier of the end device (lowercase, alphanumeric with dashes, max 36 characters)
  - tyness # look in windsensor.yaml for the end device definition
  - tynode # look in windsensor.yaml for the end device definition
  - squid # look in windsensor.yaml for the end device definition
  - ambiance # look in windsensor.yaml for the end device definition
